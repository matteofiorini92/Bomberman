package model;

/**
 * class used to group and separate items (e.g. power ups, exit) from other types of elements
 * @author Matteo
 *
 */
public abstract class Item extends Element {


	public Item(int[] position)
	{
		super(position);
	}

}
